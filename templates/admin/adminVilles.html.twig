{% extends 'admin/index.html.twig' %}



{% block title %}Hello AdminController!{% endblock %}

{% block div %}

    <h2>Gestion des villes</h2>

    <form action="{{ path('app_admin_site') }}" method="get">
        <div class="form-group">
            <label for="keyword">Rechercher :</label>
            <input type="text" name="keyword" id="keyword" class="form-control"><br>
            <button type="submit" class="btn btn-primary">Rechercher</button>

        </div>
    </form>
    <br>
    <button class="btn btn-secondary" id="addFormBtn">Ajouter une ville</button>
    <!-- Formulaire d'ajout de ville -->
    <div id="addForm" style="display:none;">
        <h3>Ajouter une ville</h3>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Ajouter</button>
        {{ form_end(form) }}
    </div>

    <br>
    <br>
    <table class="table table-dark">
        <thead>
        <tr>
            <th scope="col">Ville</th>
            <th scope="col">Code postal</th>
            <th scope="col">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for ville in villes %}
        <tr>
            <td>{{ ville.nom }}</td>
            <td>{{ ville.codePostal }}</td>
            <td>
                    <button class="btn btn-danger"><a href="{{ path('app_admin_site_delete',{'id' : ville.id }) }}">Supprimer</a></button>
                    <button class="btn btn-secondary"><a href="{{ path('app_admin_site_update',{'id' : ville.id }) }}">Modifier</a></button>
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
